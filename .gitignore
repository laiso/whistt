.build/
.swiftpm/
Package.resolved
.DS_Store
.env
debug.log

# Xcode per-user state. Shared scheme lives under xcshareddata/ and IS tracked.
xcuserdata/
*.xcuserstate

# Xcode build output (xcodebuild -derivedDataPath build)
build/

# Local recordings / scratch
*.mov
